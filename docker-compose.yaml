services:

  # --- zookeeper ---

  zookeeper:
    image: docker.io/bitnami/zookeeper:3.9
    ports:
      - "2181:2181"
    environment:
      - ALLOW_ANONYMOUS_LOGIN=yes
    networks:
      - application

  # --- kafka ---

  kafka:
    image: docker.io/bitnami/kafka:3.4
    ports:
      - "9092:9092"
      - "9093:9093"
    environment:
      - KAFKA_CFG_ZOOKEEPER_CONNECT=zookeeper:2181
      - ALLOW_PLAINTEXT_LISTENER=yes
      - KAFKA_CFG_LISTENER_SECURITY_PROTOCOL_MAP=CLIENT:PLAINTEXT,EXTERNAL:PLAINTEXT
      - KAFKA_CFG_LISTENERS=CLIENT://:9092,EXTERNAL://:9093
      - KAFKA_CFG_ADVERTISED_LISTENERS=CLIENT://kafka:9092,EXTERNAL://localhost:9093
      - KAFKA_CFG_INTER_BROKER_LISTENER_NAME=CLIENT
    depends_on:
      - zookeeper
    networks:
      - application

  # --- postgres ---

  postgresql:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust
    networks:
      - application

  liquibase-migrations:
    image: liquibase/liquibase:4.25
    depends_on:
      postgresql:
        condition: service_started
    command:
      - --changelog-file=changelog-master.yaml
      - --driver=org.postgresql.Driver
      - --url=jdbc:postgresql://postgresql:5432/postgres
      - --username=postgres
      - --password=postgres
      - update
    volumes:
      - ./liquibase:/liquibase/changelog
    networks:
      - application

  # --- redis ---

  redis:
    image: redis:latest
    ports:
      - "6379:6379"
    networks:
      - application

  # --- mongodb ---

  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: user
      MONGO_INITDB_ROOT_PASSWORD: password
    networks:
      - application

  # --- service applications ---

  service-filtering:
    build:
      context: ./jar-files/Filtering
      dockerfile: FilteringDockerFile
    depends_on:
      postgresql:
          condition: service_started
      liquibase-migrations:
          condition: service_completed_successfully
      kafka:
          condition: service_started
    networks:
      - application

  service-deduplication:
    build:
      context: ./jar-files/Deduplication
      dockerfile: DeduplicationDockerFile
    depends_on:
      postgresql:
          condition: service_started
      liquibase-migrations:
          condition: service_completed_successfully
      redis:
          condition: service_started
      kafka:
          condition: service_started
    networks:
      - application

  service-enrichment:
    build:
      context: ./jar-files/Enrichment
      dockerfile: EnrichmentDockerFile
    depends_on:
      postgresql:
          condition: service_started
      liquibase-migrations:
          condition: service_completed_successfully
      kafka:
          condition: service_started
      mongodb:
          condition: service_started
    networks:
      - application

  service-management:
    build:
      context: ./jar-files/Managment
      dockerfile: ManagmentDockerFile
    ports:
      - 8081:8081
    depends_on:
      postgresql:
          condition: service_started
      liquibase-migrations:
          condition: service_completed_successfully
    networks:
      - application

networks:
  application: {}